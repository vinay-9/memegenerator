export function reducer(state,action)
{
    switch(action.type){
        case 'LOAD_STATE':
            return 
            ...state
        break;

        default:
            return state;
    }
}

export default reducer;